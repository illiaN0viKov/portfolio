import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Illia Novikov - Full-Stack Developer",
  description: "Portfolio of Illia Novikov - Full-Stack Web Developer. Showcasing production-ready projects built with Next.js, React, Django, and more.",
  keywords: "developer, full-stack, Next.js, React, TypeScript, web development",
  authors: [{ name: "Illia Novikov" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
